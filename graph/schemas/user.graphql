
type User {
  id: String!
  name: String!
  email: String
  phoneNumber: String
}

type Query {
  users: [User!]
}

input NewUser {
  name: String!
  email: String
  phoneNumber: String
}

type Mutation {
  createUser(input: NewUser!): User!
}
