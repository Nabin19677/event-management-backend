
type EventAttendee {
    eventAttendeeId: Int!
    eventId: Event
    userId: PublicUser
}

input NewEventAttendee{
    eventId: Int!
    userId: Int!
}

extend type Query {
     getEventAttendees(eventId: Int!) : [EventAttendee!]  @requireOrganizerRole(role: [Admin, Contributor]) @authenticate
}